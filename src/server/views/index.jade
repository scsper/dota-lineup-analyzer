doctype html
html(lang='en')
    head
        title='Dota Lineup Analyzer'
        script(src='bundle.js' type='text/javascript')
        link(rel='stylesheet', href='styles/index.css', type='text/css')
        // if developing offline, require `normalize.css` from the styles directory
        link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.3/normalize.min.css',
            type='text/css')
        link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Gentium+Book+Basic',
            type='text/css')
        link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato',
            type='text/css')

        script(type='text/javascript').
            window.heroes = !{JSON.stringify(heroes)};
            window.patchToLeagues = !{JSON.stringify(patchToLeagues)};
            window.tournamentsForCurrentPatch = !{JSON.stringify(tournaments)};
            window.currentPatch = !{JSON.stringify(currentPatch)}
    body
        #container
